services:
  caddy:
    image: caddy:2-alpine
    ports:
      - "${CADDY_PORT:-80}:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
    depends_on:
      - frontend
      - backend
    networks:
      - blog-net

  backend:
    volumes:
      - ../backend:/app
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload

  frontend:
    volumes:
      - ../frontend:/app
      - /app/node_modules
      - /app/.nuxt
    environment:
      - NUXT_THEME=${NUXT_THEME:-yarb-dark}
    entrypoint: ["/app/docker-entrypoint.dev.sh"]
    command: npx nuxt dev --host 0.0.0.0

  redis:
    command: redis-server --save "" --appendonly no

networks:
  blog-net:
    driver: bridge
